{
  "name": "agent",
  "private": true,
  "scripts": {
    "build": "bun build ./index.ts --outdir=dist --target=bun",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@langchain/core": "^0.3.43",
    "@langchain/ollama": "^0.2.0"
  }
}
